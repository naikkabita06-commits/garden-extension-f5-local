{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "gardener-extension-f5.name" .) }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "gardener-extension-f5.labels" . | indent 4 }}
{{- end }}
