apiVersion: v1
kind: Secret
metadata:
  name: cmp-f5-credentials
  namespace: garden-local
type: Opaque
stringData:
  # Choose ONE auth mode:
  #
  # Mode A) Basic auth (required keys: username, password)
  # username: "REPLACE_ME"
  # password: "REPLACE_ME"
  #
  # Mode B) Ce-Auth token auth (required keys: Ce-Auth, project-id)
  #
  # Mode C) API key credentials — RECOMMENDED for auto token rotation
  #         automatic token rotation. The extension controller generates fresh
  #         short-lived Ce-Auth tokens from these long-lived keys on every
  #         reconcile (~5 min validity).
  
   # Option 1: API key credentials (auto-rotation)
  api-key-id: "fa5ee955-0b3a-412a-8a68-d7f39978840b"
  api-secret: "hxy00x7HVC8a/B5vJ9lTh94pX46sc9iy-wM1oPzH"
  project-id: "qa-first-cell"
  organisation-name: "qa-tenant"  # optional; defaults to spec.tenantOrPartition
  
  # Mode B example (manual rotation — not recommended)
  # Ce-Auth: "REPLACE_ME"
  # project-id: "REPLACE_ME"
  # organisation-name: "REPLACE_ME"  # optional; defaults to spec.tenantOrPartition