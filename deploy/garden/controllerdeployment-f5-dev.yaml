apiVersion: core.gardener.cloud/v1
kind: ControllerDeployment
metadata:
  name: gardener-extension-f5

helm:
  ociRepository:
    pullSecretRef:
      name: provider-local-airtel-registry-pull
    repository: b2bimagerepo.airteluat.cloud/gkaas/gardener-extension-f5
    tag: 0.1.2

  values:
    image:
      repository: b2bimagerepo.airteluat.cloud/gkaas/gardener-extension-f5
      tag: dev-v0.1.0

    imagePullSecrets:
      - name: airtel-registry-pull

    rbac:
      clusterAdmin: true